<form method="post" action="{{ path('app_fr_categorie_cat_delete', {'id': fr_categorie_cat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fr_categorie_cat.id) }}">
    <button class="btn">Delete</button>
</form>
