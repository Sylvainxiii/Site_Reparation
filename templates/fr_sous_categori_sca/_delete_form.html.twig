<form method="post" action="{{ path('app_fr_sous_categori_sca_delete', {'id': fr_sous_categori_sca.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fr_sous_categori_sca.id) }}">
    <button class="btn">Delete</button>
</form>
