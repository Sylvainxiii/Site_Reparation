<form method="post" action="{{ path('app_fr_pieces_detachees_pid_delete', {'id': fr_pieces_detachees_pid.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fr_pieces_detachees_pid.id) }}">
    <button class="btn">Delete</button>
</form>
