<form method="post" action="{{ path('app_fr_intervention_int_delete', {'id': fr_intervention_int.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fr_intervention_int.id) }}">
    <button class="btn">Delete</button>
</form>
